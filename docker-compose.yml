services:

  fastapi:
    build:
      context: .
      dockerfile: docker/fastapi/Dockerfile

    container_name: fastapi

    ports:
      - "8000:8000"

    volumes:
      - ./app:/app

    restart: always